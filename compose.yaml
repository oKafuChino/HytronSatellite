services:
  hytron-bot:
    build: .
    container_name: hytron-satellite-bot
    env_file:
      - .env
    restart: unless-stopped
    init: true
    volumes:
      - ./data:/data
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"
